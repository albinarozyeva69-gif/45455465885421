# Admin Notes

The production admin interface lives at `/admin` and uses Supabase Auth, Row Level Security, and the `prompt-images` storage bucket.

Local development without Supabase runs in demo mode so the mobile admin workflow can be tested instantly.
